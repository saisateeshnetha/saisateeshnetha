import React from 'react'

function Admin(props) {
    return (
        <div>
            <h1>Admin</h1>
            <h4>welcome to (props.username)</h4>
            <p>Logout</p>
        </div>
    )
}

export default Admin
